# example-plugin

Template plugin. Copy this folder to start a new plugin.

Contents: `.claude-plugin/plugin.json` (manifest), `skills/example-skill/SKILL.md` (skill template).

Optional additions: `commands/` (slash commands, one `.md` per command), `agents/` (subagent definitions), `hooks/hooks.json`.
